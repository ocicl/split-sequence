on: [push]

jobs:
  ocicl_job:
    permissions:
      contents: read
      packages: write
      id-token: write
    runs-on: ubuntu-latest
    name: Test and publish package
    steps:
      - id: test-and-publish
        uses: ocicl/ocicl-action@main
